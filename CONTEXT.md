# WMS 领域词汇表

仓库管理系统：采购→入库→销售 的一体化库存与单据流程。本表只收录本项目特有的、易混淆的领域词。

## 价格

**进价（purchasePrice）**:
商品（物料）主数据上的**现行采购参考价**（最近一批进价），也是「物料管理」页进价列与库存成本核算的基础。由采购部门维护，仓储角色不可见、不可改。**只存单一当前值，不承载历史**。
_Avoid_: 成本价, 采购价（当指明细成交价时）

**每批采购价（历史）**:
同商品同供应商每次采购的实际成交单价，按批记录在各进货单/采购申请单明细上，通过「商品进货」页按进货单查看；**不建独立价格历史表**。

**采购单价（unitPrice）**:
采购申请单/进货单**明细**上记录的本次实际成交单价，由采购人员在处理单据时填写；在采购申请「确认入库」时**回写**为该商品的最新「进价」。
_Avoid_: 单价, 成交价

**最近一批进价**:
同一商品多次采购价不同时，「进价」显示最近一批「确认入库」的「采购单价」；历次各批单价保留在进货单/申请单明细，不保留独立历史价格表。

## 单据

**采购申请单（PurchaseRequest）**:
采购需求，含主单与多商品明细；经 待采购→采购中→待入库确认→已入库 流转。价格在「到货提交」由采购填写。按来源分两类：仓储发起（默认）、生产补料发起（`sourceType=production`）。**生产补料来源的采购申请不经仓储转正，发起即待采购**（D59）。
_Avoid_: 请购单

**认领（process）**:
采购 admin 接收采购申请单的动作，状态由待采购转采购中。本动作只记录「预计到货时间」，**不填价格**——价格统一在「到货提交」。

## 销售 / 客户

**客户公司名（customerName）**:
销售单上的**自由文本**客户名（无客户主档表，仅作为 `biz_sales.customer_name` 字符串存储）。同一实体，按业务阶段取不同称呼：采购/仓储侧叫「物料」，销售/销售退货侧叫「商品 / 客户」。
_Avoid_: 用独立客户/公司主档表（本项目不建；如需再去重提示，由前端按已有的来源销售单客户名去重）

**是否含税（taxIncluded）**:
销售单上的**纯记录性布尔标志**（0-不含税, 1-含税），只记录报价口径，**不影响销售总额/单价计算**（总额 = 单价×数量，不折算税额）。历史单据默认 0。
_Avoid_: 税率字段、税额计算（本项目未引入）

**退货公司名（customerName on sales_return）**:
销售退货单上的客户公司名快照，建退货单时从来源销售单带出（或前端手动填写），存 `biz_sales_return.customer_name`。系统无客户主档，退回的客户即来源销售单的客户。

## 生产

**成品（FinishedGoods）**:
`base_goods.type = 成品` 的记录，是可销售、可被生产组装的产品（如 PTO153）。销售卖成品；生产按 BOM 组装它。与「物料」并列的货品形态二分，由 D41 引入。**成品主档由 BOM 建档生成**——生产研发部建 BOM 时按"成品名"自动落一条 `type=product` 记录，用户不手工建成品、也看不到成品编码/供应商（内部挂缺省供应商）。领料按 BOM 展开即可（D46）。
_Avoid_: 把"属于哪个成品"的语义塞进 `product_name`；避免要求"先在商品资料录入成品、再做 BOM"

**物料 / 零件（Material）**:
`base_goods.type = 物料` 的记录，是采购购入的零件/零件，存放在仓库，作为成品 BOM 的组成部分。采购侧沿用"商品/物料"称呼；生产域新建 BOM 后，同一 `base_goods` 按 `type` 语义区分。

**BOM / 物料清单**:
一张"成品 → N 行物料明细"的子表；每行 = 物料(goods_id) + 单台用量 + 规格/材质/备注。决定生产一个成品需要从仓库出哪些料、各多少。由生产研发部（前身研发）维护（D37/D45）。**一个 BOM 即一种成品**：BOM 编码自动生成「{成品名称}-BOM」（如 PTO153-BOM），新建 BOM 只填成品名+单位+明细，不再选成品（D46）。
_Avoid_: 在货品表上用 `product_name` 人工维护"成品—物料"关系；在 BOM 表单里让用户挑成品

**单台用量**:
BOM 明细上该物料在单个成品里的使用量（如 PTO153 每台用轴承珠 2 个）。齐套预警按「单台用量 × 生产数量」折算总需求。

**齐套预警**:
建生产任务单时，按「成品 × 数量 × BOM 展开」得到物料总需求，与 `base_goods.stock` 对比：够→正常；部分缺→标红/黄；严重缺→需补料。解决"生产到一半发现仓库缺料、工期延误"的核心痛点（D42）。**齐套（ok）是生产申请领料的口径**——库存已足够该成品 BOM 全部用量（含补料入库后补齐）才允许领料。

**生产任务单（ProductionOrder）**:
生产研发部管理员建的工单，含成品、数量、工序清单(静态)、状态；建单即触发齐套预警，后续走 补料(缺料时)→领料→生产→质检→成品入库。10 道装配工序中有 8 道只是静态清单（SOP 打印，不逐步追踪），2 道测试工序单独落库（D40）。**开工前置**：该成品已创建领料申请且已全额出库（D59）；开工不再自动发料。
_Avoid_: 把 10 道工序全部做成逐步报工的 MES

**质检记录（QC_Record）**:
首测、成品测这两道测试工序落库的记录：测试员/结果(OK/NG)/时间/原因。结果为 NG 触发返工→重测；重测合格才允许生产入库；不可修复则报废（D40）。

**返工 / 重测 / 报废**:
质检不合格的处置流转。返工=生产管理员重新派发修复；重测=修复后再测直到合格；报废=不可修复，终止工单并作废相应产出。

**生产补料（补料）**:
生产端发起、补足生产缺料（齐套不足）的**采购链路前端环节**。发现缺料 → 生产点补料（自选具体物料、提交）→ 直接生成 status=待采购 的采购申请（**不经仓储转正**）→ 采购到货 → 仓储确认入库（物料库存+）。生产补料即一种 `sourceType=production` 的采购申请，`sourceType` 用于标识来源，不再带仓储审批 gate（D59）。
_Avoid_: 把补料当"领料单补料类型 SUPPLY"（那是仓储手动内部补料，语义不同）；让补料再经仓储草稿转正（D59 废除）

**生产领料**:
生产人员按 BOM 从仓库领出成品所需的对应物料（物料库存 -）。**统一为"申请领料 → 仓储确认出库 → 才可开工"**：无论补料与否，生产端在生产任务单详情发起领料申请（明细按 BOM×数量自动带出、只读不可改），仓储端只对已提交的领料单做「确认出库」扣库存（D59）。领料单复用 `biz_pick_lists`（pick_type=PICK），仓储不发起/不创建/不审批生产领料。
_Avoid_: 开工时自动生成领料并自动扣库存（旧逻辑，D59 废除）；仓储手动替生产建领料单

**生产入库**:
成品组装完成且质检合格后，由生产人员把成品数量入库（成品库存 +）。本域替代原仓储端同名功能（阶段 13 迁移归属）。

**生产研发部（dept 生产）**:
新部门 `dept_code=production`（生产研发部），既研发又生产；复用三档角色——管理员=`admin+production`（建/审生产任务、维护 BOM、派发、报废处置），员工=`employee+production`（补料/领料申请、质检录入、入库、只读库存与 BOM）。BOM 建档由该部门完成（D37 承接研发职责）。**生产补料与生产领料申请均由该部门发起**；仓储端不发起/不创建/不审批这两种单，只对已提交领料单做「确认出库」扣库存（D59）。