# WMS 领域词汇表

仓库管理系统：采购→入库→销售 的一体化库存与单据流程。本表只收录本项目特有的、易混淆的领域词。

## 价格

**进价（purchasePrice）**:
商品（物料）主数据上的**现行采购参考价**（最近一批进价），也是「物料管理」页进价列与库存成本核算的基础。由采购部门维护，仓储角色不可见、不可改。**只存单一当前值，不承载历史**。
_Avoid_: 成本价, 采购价（当指明细成交价时）

**每批采购价（历史）**:
同商品同供应商每次采购的实际成交单价，按批记录在各进货单/采购申请单明细上，通过「商品进货」页按进货单查看；**不建独立价格历史表**。

**采购单价（unitPrice）**:
采购申请单/进货单**明细**上记录的本次实际成交单价，由采购人员在处理单据时填写；在采购申请「确认入库」时**回写**为该商品的最新「进价」。
_Avoid_: 单价, 成交价

**最近一批进价**:
同一商品多次采购价不同时，「进价」显示最近一批「确认入库」的「采购单价」；历次各批单价保留在进货单/申请单明细，不保留独立历史价格表。

**标准售价（salePrice）**:
成品主数据上的**挂牌售价**（`base_goods.sale_price`），由**销售部门成员**（admin+employee）在「成品管理」页以单字段编辑维护（镜像采购维护「进价」的范式；销售对主数据只有定价权，无建档/改档权）。建销售单时作为默认单价带出；销售开单临时改价与标准售价偏离超阈值仍触发超管审批（阶段 5 机制保留——主数据定价权下放 ≠ 单据让价免审）。物料不维护售价；仓储、采购不可改售价。
_Avoid_: 把销售单明细单价当标准售价回写；因定价权下放而取消偏离审批

## 单据

**采购申请单（PurchaseRequest）**:
采购需求，含主单与多商品明细；经 待采购→采购中→待入库确认→已入库 流转。价格在「到货提交」由采购填写。按来源分两类：仓储发起（默认）、生产补料发起（`sourceType=production`）。**生产补料来源的采购申请不经仓储转正，发起即待采购**（D59）。生产补料明细在提交时快照 BOM 行的规格/材质/备注，详情按【已有物料缺口】与【未知物料（新物料）】两组展示（ADR-0002）。明细行各自带「预计到货时间」与「到货备注」，由采购认领时填写（见「认领」）；整单级预计到货时间已废除（D61）。
_Avoid_: 请购单

**认领（process）**:
采购 admin 接收采购申请单的动作，状态由待采购转采购中。认领时**按明细行逐一填写「预计到货时间」（必填）与「到货备注」（选填）**，并向来源申请人推送行级到货摘要站内信；入库确认前（采购中状态）可再次修改。**不填价格**——价格统一在「到货提交」。整单级预计到货时间已废除（D61）。

**到货备注（arrivalRemark）**:
采购申请单**明细行**上采购口径的备注（供应商、发货方式、分批等到货相关信息），认领时随行级「预计到货时间」一起填写，入库确认前可修改。与明细「备注」（BOM 行物料描述快照，ADR-0002）是**两个独立字段**，互不复用。
_Avoid_: 把到货备注写进物料描述备注；在整单级维护到货信息

## 销售 / 客户

**客户公司名（customerName）**:
销售单上的**自由文本**客户名（无客户主档表，仅作为 `biz_sales.customer_name` 字符串存储）。同一实体，按业务阶段取不同称呼：采购/仓储侧叫「物料」，销售/销售退货侧叫「商品 / 客户」。
_Avoid_: 用独立客户/公司主档表（本项目不建；如需再去重提示，由前端按已有的来源销售单客户名去重）

**是否含税（taxIncluded）**:
销售单上的**纯记录性布尔标志**（0-不含税, 1-含税），只记录报价口径，**不影响销售总额/单价计算**（总额 = 单价×数量，不折算税额）。历史单据默认 0。
_Avoid_: 税率字段、税额计算（本项目未引入）

**退货公司名（customerName on sales_return）**:
销售退货单上的客户公司名快照，建退货单时从来源销售单带出（或前端手动填写），存 `biz_sales_return.customer_name`。系统无客户主档，退回的客户即来源销售单的客户。

**销售退货 vs 作废（已出库单库存回补的两条正交路径）**:
对已出库销售单，「作废」与「销售退货」都让库存 +N，但语义与路径正交。**作废** = 单据纠错：整单取消、原单退出有效销售（毛利严口径 `biz_status=1` 不含）、库存事务内自动回补（**无实物验收闸口**）、撤消息撤审批并通知关联生产（D73）、可选红冲留痕；适用「货没真出门」的开错单/客户整单取消。**销售退货** = 实物逆向回流：原单不动仍计有效销售，独立退货单**按自身时间归年冲减**（ADR-0008，不回溯原单年份）、**支持部分退/多次退**（可退量 = 原单量 − 已退累计）、库存经**仓储确认入库**才回补（实物验收闸口）；适用「货已交付客户后退回」。缺一不可：只有退货 → 开错单被迫编退货，销售额与退货率双失真；只有作废 → 跨期退回回溯动历史账、部分退无解、货已交付时无验收虚增库存。
_Avoid_: 用作废处理客户实际退货（无验收+跨期动账）；用退货单纠正开错单（账面虚增一销一退两条假流水）

**待货销售单（BackorderSales）**:
库存为 0 或不足时仍允许创建的销售单（定制公司模式：销售单是需求来源，库存只代表"现货可即发"）。建单**零库存校验**（允许超卖）；库存扣减仍在仓储「确认出库」环节硬校验兜底，缺货单自然挂在待确认队列等到货，页面给"库存不足"标识。**不拆单部分发货**——整单待到货后一次确认出库。
_Avoid_: 建单时恢复库存拦截；把一张销售单拆成多批次发货

**预计可交付时间（EstimatedDelivery）**:
履约时间线上的系统推算值，仅供参考、**非对客承诺**。推算口径：现货充足 → "立即可发"；已关联生产单且缺料中 → max（补料采购行预计到货时间）+ 标准工期；齐套/已开工 → （开工时间，未开工按当前时间）+ 标准工期；**BOM 未填工期 → 不推算，显示"待生产评估"**；未关联生产单 → 不推算，显示"待生产排产"。生产可在生产任务单上手工修正预计完工时间，修正走 @AuditLog 留痕；手工修正值优先于一切推算值。

**履约时间线（FulfillmentTimeline）**:
销售单详情内的成品履约进度视图（一单一品，直接挂在销售单上），回答客户"我订的那批货什么时候好"。固定 8 节点：下单 → 生产排产 → 物料准备（齐套✓ / 缺料→补料采购中最晚预计到货）→ 开工 → 装配进度（工序打卡 x/7）→ 质检（首测/成品测）→ 成品入库 → 发货（仓储确认出库）。已完成节点显示实际时间，未来节点显示「预计可交付时间」。现货充足的单只有 下单→发货 两节点。关联生产单已终止时，时间线回退为「待重新排产」（D73）。

**销售需求联动（SalesToProduction）**:
缺货销售单驱动生产排产的桥：销售单创建时若现货不足 → 站内信通知生产管理员"有销售需求待排产"（biz 绑定销售单，D21 范式，删除/作废时撤未读）；生产建任务单时可**选填**关联的销售单（不强制，通用备货单留空）——生产任务单挂可空 `sales_order_id`，**一张生产单最多关联一张销售单**（多做的备货量不入关联；不建多对多关联表）。销售单的履约时间线经此关联追踪生产/采购/入库进度。生产单入库且关联销售单仍待出库时，站内信通知**建单销售本人**（绑销售单 biz，出库/作废时撤未读）。不自动生成生产任务单——生产数量、批次、是否研发新 BOM 是生产的决策。销售单取消（删除/作废）时，若有关联且未终态的生产任务单，额外发消息通知生产管理员"关联销售单已取消"（绑 production_order biz，D73），由生产管理员判断是否手动终止；不做越权自动终止。
_Avoid_: 销售单库存不足时自动建生产任务单；强制每张生产单关联销售单；多对多分摊（哪台给哪单是过度设计）；销售取消直接越权终止生产单

## 生产

**成品（FinishedGoods）**:
`base_goods.type = 成品` 的记录，是可销售、可被生产组装的产品（如 PTO153）。销售卖成品；生产按 BOM 组装它。**业务下拉按形态收紧**：「销售下单」「生产入库」只可选成品，「商品进货」「采购申请」建单只可选物料。与「物料」并列的货品形态二分，由 D41 引入；**主数据页面同样二分**：成品在「成品管理」页维护，物料在「物料管理」页维护。成品主档有两个入口：在「成品管理」手工新建，或建 BOM 时按成品名自动落一条 `type=product` 记录；建 BOM 时同名成品已存在则**复用**、不重复建档（D46 已修订：不再禁止手工建成品）。成品**不参与库存预警与缺货识别**（缺货识别、首页低库存统计、库存预警查询均排除成品）。成品无供应商/进价/预警概念（内部挂缺省供应商），用户看不到成品编码。领料按 BOM 展开即可。
_Avoid_: 把"属于哪个成品"的语义塞进 `product_name`；在成品/物料管理页做 BOM 关联（成品→物料关系只在「BOM管理」维护）；把成品纳入缺货识别或库存预警

**物料 / 零件（Material）**:
`base_goods.type = 物料` 的记录，是采购购入的零件/零件，存放在仓库，作为成品 BOM 的组成部分。采购侧沿用"商品/物料"称呼；生产域新建 BOM 后，同一 `base_goods` 按 `type` 语义区分。在「物料管理」页维护（该页只显示物料）。规格/材质是物料的固有属性，落主数据字段，备注沿用主数据描述；**唯一性按「名称+规格」判定**——同名不同规格各自成条，下拉与列表须展示规格/材质防混选（ADR-0003）。

**BOM / 物料清单**:
一张"成品 → N 行物料明细"的子表；每行 = 物料(goods_id) + 单台用量 + 规格/材质/备注。决定生产一个成品需要从仓库出哪些料、各多少。由生产研发部（前身研发）维护（D37/D45）。**一个 BOM 即一种成品**：BOM 编码自动生成「{成品名称}-BOM」（如 PTO153-BOM），新建 BOM 只填成品名+单位+明细，不再选成品（D46）。BOM 明细的关联物料下拉**只可选物料**，成品不可作组件。删除 BOM 为软删；「下达生产任务单」的成品下拉只列出存在有效（未删）BOM 的成品；删除时若该成品有未完结生产任务单，须二次确认（删除后这些任务单无法再补料）；删除后其成品主档在**无库存且无任何单据引用**时随之清理，否则保留。
_Avoid_: 在货品表上用 `product_name` 人工维护"成品—物料"关系；在 BOM 表单里让用户挑成品；把成品选成 BOM 组件

**单台用量**:
BOM 明细上该物料在单个成品里的使用量（如 PTO153 每台用轴承珠 2 个）。齐套预警按「单台用量 × 生产数量」折算总需求。

**齐套预警**:
建生产任务单时，按「成品 × 数量 × BOM 展开」得到物料总需求，与 `base_goods.stock` 对比，行级匹配四态：齐套（库存≥需求）、部分缺料（已绑定、0＜库存＜需求）、严重缺料（已绑定、库存为 0）、未知物料（行未绑定物料）。存在严重缺料或未知物料时，建单即站内信通知采购管理员（含带规格的缺口明细，作废时撤回未读）。解决"生产到一半发现仓库缺料、工期延误"的核心痛点（D42）。**齐套（ok）是生产申请领料的口径**——库存已足够该成品 BOM 全部用量（含补料入库后补齐）才允许领料。补料采购单**确认入库后系统重算齐套，缺口清零即站内信通知生产部管理员「物料已齐套可领料」**（绑生产单生命周期，作废/报废时不发，D62）；仍缺料则通知「补料部分到货仍缺料」可继续补料（D87，修订 D62 沉默口径）。两通知**互斥**：重算时先按标题白名单（`revokeUnreadByBizAndTitles`）撤同 biz 的齐套类旧未读再按结果发新，「关联销售单已取消」(D73) 等其他通知不受影响，亦不堆积。

**生产任务单（ProductionOrder）**:
生产研发部管理员建的工单，含成品、数量、生产工序、状态；建单即触发齐套预警，后续走 补料(缺料时)→领料→生产→质检→成品入库。**生产工序共 10 道**（D64 定稿文案）：7 道装配工序（1 磁性材料装配 / 2 底座结构组装 / 3 手柄机构装配 / 4 PCB板焊接及安装 / 5 程序烧录 / 7 屏蔽壳安装 / 9 发合格证、条码、标签、配件及包装）由「工序打卡」追踪；第 6 首次测试 / 第 8 成品测试由质检记录驱动、第 10 成品入库由生产入库驱动（这三行展示时实时推导，不重复落库）。**开工前置**：该成品已创建领料申请且已全额出库（D59）；开工不再自动发料。
_Avoid_: 把 10 道工序做成逐步报工的 MES（报工数量/工时/派工——打卡只是完成留痕，D64 收窄 D40 口径）；把工序清单做成按成品可配置模板（单产线，暂无必要）

**工序打卡（Step_Checkoff）**:
生产研发部成员对工单 7 道装配工序标记「完成」的轻量留痕：记录打卡人/时间；本人或生产管理员可撤销；仅生产中/待入库状态可打卡与撤销。**不是流程闸口**——不拦截完工/生产入库，质量闸口仍只有质检两测点（D64）。打卡记录存 `biz_production_order_step`，仅 7 道人工工序落库。
_Avoid_: 用打卡拦截完工/生产入库；把首次测试/成品测试/成品入库也做成打卡行（它们由质检/入库状态推导，不落库）

**质检记录（QC_Record）**:
首测、成品测这两道测试工序落库的记录：测试员/结果(OK/NG)/时间/原因。结果为 NG 触发返工→重测；重测合格才允许生产入库；不可修复则报废（D40）。

**返工 / 重测 / 报废**:
质检不合格的处置流转。返工=生产管理员重新派发修复；重测=修复后再测直到合格；报废=不可修复，终止工单并作废相应产出。

**生产补料（补料）**:
生产端发起、补足生产缺料（齐套不足）的**采购链路前端环节**。发现缺料 → 生产点补料（已绑定物料直接带出、只填申请数量；未知物料行见「未知物料」）→ 直接生成 status=待采购 的采购申请（**不经仓储转正**）→ 采购到货 → 仓储确认入库（物料库存+）。生产补料即一种 `sourceType=production` 的采购申请，`sourceType` 用于标识来源，不再带仓储审批 gate（D59）。**幂等口径（D86）**：同一生产单至多一张**在途**补料单（待采购/采购中/待入库确认）；已入库/已驳回不占用名额——部分到货终态后的剩余缺口可再次补料，防重复由「当前无缺料，无需补料」实时缺口重算兜底。补料弹窗遇在途单时提示单号并禁提交（D87，详情 VO 透 `inFlightRequestNo`）。确认入库重算齐套仍缺 → 通知生产「补料部分到货仍缺料」可继续补料，与「物料已齐套可领料」互斥（按标题白名单撤旧发新，见「齐套预警」）。
_Avoid_: 把补料当"领料单补料类型 SUPPLY"（那是仓储手动内部补料，语义不同）；让补料再经仓储草稿转正（D59 废除）；要求「全部缺料行选齐才允许提交」（ADR-0002 对未知物料行修订）；把已入库补料单当在途拦截再补（D86 修订前口径，部分到货场景死锁）

**未知物料（UnknownMaterial）**:
BOM 明细行未绑定物料（`goods_id` 为空）的「方案先行」行；绑定过的物料被软删后视同未知。匹配栏独立一态，不与严重缺料混计，同样阻断开工。补料时该行**不可从已有物料下拉选择**：由生产核对/修正自 BOM 预填的名称/规格/材质/备注/单位并填申请数量，提交补料时系统自动在物料主数据建档（挂缺省供应商，进价留空由采购维护）并回绑 BOM 行；名称与规格均与既有物料相同时整单退回、冲突行改绑已有物料；同名不同规格照常建档（ADR-0002/0003）。
_Avoid_: 把未知物料当严重缺料处理；让生产从已建档物料里「就近选一个」；恢复仓储入库时定料（ADR-0001 已废除）

**生产领料**:
生产人员按 BOM 从仓库领出成品所需的对应物料（物料库存 -）。**统一为"申请领料 → 仓储确认出库 → 才可开工"**：无论补料与否，生产端在生产任务单详情发起领料申请（明细按 BOM×数量自动带出、只读不可改），仓储端只对已提交的领料单做「确认出库」扣库存（D59）。领料单复用 `biz_pick_lists`（pick_type=PICK），仓储不发起/不创建/不审批生产领料。领料/退料明细行展示的规格/材质/备注是**物料主数据口径**（备注=主数据描述，非 BOM 行备注——与补料单明细的快照不同源），建单时定格。
_Avoid_: 开工时自动生成领料并自动扣库存（旧逻辑，D59 废除）；仓储手动替生产建领料单；把领料明细备注当 BOM 行备注

**生产入库**:
成品组装完成且质检合格后，由生产人员把成品数量入库（成品库存 +）。本域替代原仓储端同名功能（阶段 13 迁移归属）。

**标准工期（leadDays）**:
BOM 上的标准生产周期（天），由**生产部门成员**在建立/编辑 BOM 时填写，**可空**——定制新品不知道工期就留空，不设全局默认值。留空时系统不推算「预计可交付时间」，时间线显示"待生产评估"，由生产在生产任务单上手工填预计完工时间兜底。只存天数整数，不含排产日历/班次。
_Avoid_: 设全局默认工期（拍脑袋值会污染推算）；精确到小时

**手动终止（生产任务单）**:
生产任务单的第七种终态（status=7），语义是「做了一半不做了」——区别于作废（5=单据不该存在）与报废（6=质检不合格）。仅生产管理员可执行（1/2/3 状态可进入，不可逆，原因必填，@AuditLog）。终止时一个事务内生成 RETURN 退料单（退料量以「已领未退」净额为上界服务端兜底），由仓储确认回流入库；已有进行中退料单则跳过自动生成、提示人工核对。已终止单不参与 UNFINISHED_STATUSES（齐套/预警/补料不受影响）；打卡/质检冻结但记录保留可见；履约时间线回退「待重新排产」。销售单取消时，关联的未终态生产单会收到通知，但是否终止由生产管理员判断（D73）。
_Avoid_: 复用「作废」语义（作废会清空步骤实例展示且含义是"不该存在"）；允许销售/仓储直接终止生产单（越权）

**已领未退（终止退料净额）**:
生产任务单终止时计算的退料上界——按物料汇总：（PICK 已发料 + SUPPLY 已发料）−（RETURN 已发料）。终止弹窗预填该净额，可减少不可加；服务端兜底校验，防多退虚增库存。口径与 createReturn 可退量计算一致。

**生产研发部（dept 生产）**:
新部门 `dept_code=production`（生产研发部），既研发又生产；复用三档角色——管理员=`admin+production`（建/审生产任务、维护 BOM、派发、报废处置），员工=`employee+production`（补料/领料申请、质检录入、入库、只读库存与 BOM）。BOM 建档由该部门完成（D37 承接研发职责）。**生产补料与生产领料申请均由该部门发起**；仓储端不发起/不创建/不审批这两种单，只对已提交领料单做「确认出库」扣库存（D59）。

## 消息

**消息跳转目标（targetRoute）**:
站内消息自带的跳转路由（D75）——接收方点击消息该去的列表页，由发送方在发消息时显式指定（「接收方该去哪处理」）；为空时前端按 bizType 候选映射兜底（存量消息）。bizType 只承载消息生命周期（D21 撤销匹配），不承载跳转语义。
_Avoid_: 按角色猜路由、把 bizType 当路由用

## 统计

**已实现销售毛利（GrossProfit）**:
财务统计口径的毛利 = **净销售额 − 销售成本快照**（销售单/退货单上的 `cost_total_price`），只覆盖已生效出库的销售（严口径：`biz_status=1 AND confirm_status=2 AND is_deleted=0`）。**不是**「销售额 − 当年采购额」的收支差——后者混入库存变动，不称毛利。销售统计图表毛利视角与年度经营统计（ADR-0008）共用此口径，数字可交叉验证。
_Avoid_: 收支差、现金毛利

**年度归属（统计归年）**:
年度统计中单据计入哪个自然年的规则（ADR-0008）：销售单/销售退货按各自 `operation_time`；进货单按 `COALESCE(confirm_time, operation_time)`（钱货两清的入库确认时点）；采购申请按主表 `COALESCE(confirm_time, receive_time)`；采购退货按 `COALESCE(complete_time, operation_time)`。**退货按退货单自身时间归年冲减，不回溯原单年份**（允许负值行）。
_Avoid_: 按创建时间归年；退货回溯冲减原单年份

## 盘点

**盘点单（Stocktake）**:
仓储对库存商品做账实核对的单据（对应客户需求痛点 4「盘点耗时费力」）。建单时由仓储管理员**自选商品范围**（物料/成品均可，可全选即全盘），随后录入实盘数量，经仓储管理员审核后按差异调整库存。状态机：`盘点中`（可录入/取消）→ 提交 → `待审核`（可审核生效/驳回重录/取消）→ `已完成`（终态）；另有 `已取消`。**已完成终态不可作废、全程无删除入口**（只有取消）——盘点是审计敏感区，盘错了的正确姿势是再盘一次纠正，单据不该有"消失"路径。盘点属仓储内部流程，**无跨部门审批层、不发站内消息**（双人制衡靠「录入≠审核」分工 + 审计日志留痕）；明细**不含金额列**（财务成本影响走既有报表口径）。明细行建单时快照商品名称/规格/材质/单位（对齐 D60/D63 范式），同名不同规格物料（ADR-0003）可区分。
_Avoid_: 库存调整单、损益单；作废/删除已完成盘点单；盘点单带金额

**账面快照（bookQty）**:
盘点建单时点各商品库存数的定格记录，作为盘点的账面**展示/核对参考**。盘点期间业务**正常出入库不冻结**（冻结全天业务不现实）。注意：快照**不是**差异调整基准——见「差异调整基准」。
_Avoid_: 盘点期间锁定库存、冻结出入库；把快照当调整基准（并发业务下会重复调整）

**差异调整基准**:
盘点审核生效时计算差异的账面锚点 = **生效时点的实时库存**（差异 = 实盘数 − 生效时账面），而非建单快照——若按快照基准，盘点期间发生的出入库会被重复调整。明细行记录三值：快照账面 / 实盘数 / 生效时账面。
_Avoid_: 实盘 − 快照（期间业务重复入账）

**未盘行**:
盘点单中提交时仍未录入实盘数的明细行。**允许部分提交**（至少录一行即可），审核只对已录行生效，未盘行标「未盘」不调整库存，其「上次盘点时间」不刷新，自然进入下轮待盘——支撑人工循环盘「分区轮盘」用法。
_Avoid_: 强制全单录完才能提交

**上次盘点时间**:
商品最近一次**审核生效**的盘点明细行的生效时间，**查询派生**（非 base_goods 加列，无双写漂移）。展示在盘点建单勾选列表并按其排序（最久未盘排最前），辅助人工循环盘。
_Avoid_: base_goods 冗余列回写

**盲盘（BlindCount）**:
盘点防作弊标准做法——导出给盘点人的纸质盘点表**默认不含账面数**（防照抄账面），实盘数录入系统后才见账面与差异；可切换为显示账面数（明盘）。导出表同时是**实盘回填模板**：盘点人填好实盘列后可将 xlsx 直接导回系统批量回填（页面逐行录入亦可用），闭环解决「盘点耗时费力」的录入效率一半。
_Avoid_: 默认把账面数印在盘点表上；导出与导入两套格式

**循环盘点（CycleCount）**:
分批次轮流盘点的作业方式（客户需求痛点 4 点名）。一期**不做自动排程**，以每个商品的「上次盘点时间」辅助仓储人工安排轮盘；自动周期任务留待二期增量。
_Avoid_: 一期引入定时生成盘点任务的排程机制

**盘盈 / 盘亏**:
审核生效时的两种差异方向：实盘 > 账面 → 盘盈（库存调增）；实盘 < 账面 → 盘亏（库存调减）。差异调整与业务出入库走同一库存变更纪律（服务内私有助手），并全程审计留痕。
_Avoid_: 绕过服务直接改库存字段

**盘点负责人（Assignee）**:
盘点单每个明细行**建单时逐行指定**的责任盘点人（每行必选，候选 = 仓储部门启用成员 admin+员工）。**员工仅可录入本人负责的行，仓储管理员可录任意行兜底**；盘点中管理员可**改派**（人不在岗时调整）。这是「计划上谁该盘」。
_Avoid_: 建单后再补分配；员工自由录入任意行；把负责人与实际录入人混为一谈

**实际录入人（Counter）**:
明细行实盘数的**真实录入者与录入时间**（页面逐行录入和 xlsx 导入回填都盖章），可能与负责人不同（如管理员代录）。配合主单的建单人/提交人/审核人/取消人，构成盘点全程「谁盘的、谁送的审、谁审的、谁取消的」操作人链路。这是「实际上谁盘的」。
_Avoid_: 只记负责人就当有追溯；录入不盖时间

## 删除

**逻辑删除（软删除，is_deleted）**:
系统内几乎所有「删除」动作（前端删除按钮、DELETE 接口）都是**逻辑删除**：MyBatis-Plus 全局配置把行标记 `is_deleted=1`，行仍留在库中，查询自动过滤——已删账号无法登录、已删数据不再出现，但记录并未消失。**物理删除（真删行）只在两处发生**：超管手动删除登录/操作日志（审计口径"删了就该真删"）、数据库级运维（备份/清库）。注意**唯一键不含 `is_deleted`**（如 `sys_user.uk_username`）——逻辑删除的同名记录仍占唯一键，同名重建会撞唯一约束，须物理清理或换名。
_Avoid_: 以为前端删除 = 数据从库里消失；给唯一键叠加 is_deleted 做"可同名再建"（当前未采用）

**删除 vs 作废（单据纠错二选一）**:
业务单据的两种取消路径，按「错发现得多早」分工。**删除** = 当天建错当天擦：仅 `operation_time` 为当天的单可删（超窗报错「历史单据请走作废/红冲流程」），逻辑删除后列表消失、无审批；权限收口部门 admin（员工仅可删本人建的未出库当天单——价格偏离被驳回后改价重提场景）。**作废** = 生效后取消、留痕：状态 正常→已作废（记作废时间+原因），单据仍在列表可见；历史单由销售 admin 发起、**仓储 admin 审批执行**（作废审批流）。两者对库存同口径：**已确认出库才回补库存**，未出库不动；都撤未读消息与待办审批（D21）。已作废单/红冲单二者皆禁（不可重复取消）。
_Avoid_: 让历史单走删除（时间窗硬拦）；把作废当无痕删除（作废恰恰是为了留痕审计）

**红冲（RedFlush）**:
已出库销售单作废时可选生成的**负数量对冲单**（biz_status=3，数量/金额为原单负值、确认状态=已出库、备注「红冲来源:原单号」）：财务账面留一笔反向流水，而非让金额凭空消失。红冲单本身禁删除/禁再作废。
_Avoid_: 改原单金额对冲；让红冲单参与删除/作废

## 审计

**超级管理员（superadmin）**:
全系统唯一账号（`role=superadmin`，DB 唯一约束保证仅一个），不属任何部门。**治理/审计角色——对业务数据只读**：可读全部业务模块页面与数据（含进价/售价列），但业务写接口对其统一拒绝（真只读，不碰业务数据）；保留的写权仅限系统管理（用户/公告/系统参数/安全策略）与治理审批（价格偏离审批/作废审批/部门审批）。财务两模块（利润分析/年度经营统计）对其也不开放——成本利润仅财务部门管理员可见。
_Avoid_: 让超管代管业务数据编辑（2026-09-13 前超管可编辑商品资料，已收回）；把财务模块对超管放开

**操作日志（sys_operation_log）**:
业务写操作的审计记录：谁/何时/对什么单据/做了什么/来源 IP。仅超管可见。**永久保留，不做任何自动清理机制**（审计数据价值在长期追溯，存储成本可忽略）；但超管可在页面上**手动删除**（物理删除，删后不可恢复），删除动作本身会写一条操作日志留痕。与「登录日志」（sys_login_log，记录登录成功/失败/IP/UA）是两张独立的表——登录日志同样永久保留+可手动删除，但不提供导出下载。
_Avoid_: 定时任务自动清理日志；把登录日志当操作日志展示；逻辑删除日志（删了就该真删）

**操作描述（detail）**:
操作日志上的**人类可读摘要**，写入时生成并落库定格（如「确认出库 销售单SAL…（PTO153×5），库存500→495」），不因日后路由/参数改名而失效。历史旧记录无描述时，前端兜底显示「模块+动作」原值。配合「变更前后快照」（before_data/after_data，关键字段变更前后的 JSON 快照，同为写入时定格）回答"这次操作到底干了什么"。
_Avoid_: 前端按 requestUri 映射表翻译（路由一改即失效，且无数据快照）

**异常操作提醒**:
【**功能挂起**（2026-09-13 定案"先不做"）】曾设想：库存单次变动超阈值/价格偏离超阈值/敏感操作（删除单据、作废通过、账号权限变更）发生时站内信提醒超管并日志标红；阈值草案（单次库存变动>500、价格偏离±30%）一并挂起，恢复时重新定议，不沿用上数。